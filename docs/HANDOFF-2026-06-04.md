# Handoff — Legendary Word Wiki (2026-06-04)

Read this first, then the spec. This captures **session/operational state** the spec doesn't.

## Where the work lives
- **Worktree:** `/Users/theoutsider/wordul-wiki` on branch **`wiki`** (isolated from a concurrent session that holds `main` + `chore/scrub-wordle-trademark`). Do NOT work in `/Users/theoutsider/wordul` — another session uses it.
- **The shell resets cwd to `/Users/theoutsider/wordul` between every command** — always `cd /Users/theoutsider/wordul-wiki && …`. (This is `fish`; no heredocs — use `browser-harness -c "…"`.)
- **Source of truth:** `docs/superpowers/specs/2026-06-04-legendary-multilingual-word-wiki.md` (the legendary 12-language vision). It supersedes/extends `2026-06-01-word-wiki-design.md`.
- **Extra ideas (durable):** `docs/superpowers/specs/2026-06-04-legendary-word-wiki-bonus-angles.md` (10x/adjacent angles; may still be writing when you read this).

## What is DONE and committed on `wiki` (base wiki — the v1, English-only)
All built, reviewed, fixed; **201/201 tests pass, typecheck clean, routes + stats API verified live:**
- Static page per answer word `/word/<slug>` (def, fact, quote, etymology, FAQ, related-word graph, JSON-LD, OG meta) — `scripts/gen-word-pages.mjs` + `scripts/lib/{word-page,word-graph,og-card,words}.mjs`.
- Worker routes (`src/worker.ts`): `/word/<slug>`, `/word/og/<slug>.png` (R2 `OG` bucket), `/words` index, `/today` redirect, `/api/word/<w>/stats`, word URLs in `/sitemap.xml`. NOTE: wrangler serves matching static assets **before** the worker; the `/word/` worker branch only runs on misses (uppercase→lowercase 301, friendly 404).
- Per-word live stats: `WordStats` DO (`src/wordstats-do.ts` + pure `src/wordstats.ts`), batched-atomic finish-hook in `src/room.ts`, `wrangler.jsonc` `WORDSTATS` binding + `v3` SQLite migration.
- `src/words.ts` (ANSWER_WORDS = **2315**, all 5-letter; `isWordPage`/`slugFor`/`wordOfTheDay`), `src/word-exclusions.ts` (empty — fill before public launch).
- End-card reveal links to `/word/<word>` (not Google) — `public/app.js`. `public/word-page.{css,js}`. `llms.txt` updated.
- `scripts/gen-word-intel.mjs` has a **`--local` Ollama backend** (`--model qwen3:14b`), quotes hard-blanked in local mode. Schema extended: `etymology`, `pos`, `syllables`.

## What is NOT done (the legendary expansion — implement from the spec)
- **Corpus content.** Only **6 hand-seeded words** have intel (POWER, DREAM, LIGHT, OCEAN, TRUTH, MONEY). The local-model corpus run was **stopped and discarded** — it was sub-legendary (bland/empty facts, fabricated etymology). The new plan = **Opus 4.8 agent teams via Workflow** producing the rich 12-language cards with adversarial verification. See spec §"Generation architecture" and §"Content model".
- **The whole 12-language / rich-card schema** (schemaVersion 2), per-language data files, `/{lang}/word/<slug>` routing + hreflang, localized sitemaps/JSON-LD/OG. Not started.
- Pages were generated once for testing then **cleaned** (not committed) — they're regenerated at build time.

## Open decisions to confirm (top of spec)
Exact 12-language set (spec picked `en es fr de pt ja ko zh-Hans hi ar ru id`), per-language content depth, accuracy/cost ceilings. Confirm before the big generation run.

## Operational gates (need user action / secrets)
- **Workflow scale:** 12-core host → **10 agents concurrent, 1000/run**. The ~27,780 word×lang cards need batched, multi-run, resumable generation (spec covers topology).
- **OG cards → R2:** wrangler's OAuth session **lacks R2 scope** (verified). User chose **"mint R2 creds via browser"** (browser-harness on their logged-in Cloudflare) at upload time. OG is non-critical — pages work without it (`og:image` 404s gracefully).
- **Deploy:** user said **"merge + deploy when ready"** but wants an **explicit go** at the deploy moment. Another agent put `CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_API_TOKEN` (Edit-Workers scope, incl. R2) into **GitHub secrets** for the CI/deploy side. Use `/push` (decoupled GitHub + Cloudflare; no CI auto-deploy).
- **Merge coordination:** `main` diverged (concurrent session). Merging `wiki` → `main` will touch `app.js`/`worker.ts`/`llms.txt` which the trademark-scrub also edited.

## Branch cleanup pending
- `feat/word-wiki` — original branch, still carries a stray `8873ba9` trademark-scrub commit (not mine).
- `trademark-scrub-rescue` — preserves `8873ba9` so the other session can recover it.
Clean both up once the wiki ships.

## Resume checklist
1. `cd /Users/theoutsider/wordul-wiki` (branch `wiki`); read the spec.
2. Confirm the open decisions with Zang.
3. Implement per the spec's phased build sequence (schema v2 → English source generation via Opus Workflow + verify → per-language → render/routing → SEO).
4. Generate + commit pages; mint R2 creds via browser + upload OG; get explicit deploy go; `/push`.
