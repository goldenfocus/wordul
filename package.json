{
  "name": "golden-wordul",
  "version": "1.0.0",
  "description": "Wordul — race your friends on the same word. No login, just a shared link.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "check-graph": "vitest run test/module-graph.test.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "voice:render": "node scripts/voice/render.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251101.0",
    "jsdom": "^29.1.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.7",
    "wrangler": "^4.83.0"
  }
}
