# Dependencies — bare pattern (no trailing slash) also ignores the per-worktree
# `node_modules` *symlink*; a dir-only `node_modules/` let the symlink get committed.
node_modules

# Claude Code worktrees (dispatched/isolated sessions)
.claude/worktrees/

# Build output
dist/
.output/

# Wrangler / Cloudflare
.wrangler/
.cf-pull/
.dev.vars
.dev.vars.*
wrangler.toml.bak

# Environment & secrets
.env
.env.*
*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Playwright MCP test artifacts
.playwright-mcp/

# stray local artifacts (not part of the app)
.claire/
wrangler.v2.jsonc
.gstack/
